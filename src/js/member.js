// ==========================================================================
// Member Info Box
// ==========================================================================

export default class Member {
  constructor(name, id, icon, age, dob, town, instrument) {
    this.name = name;
    this.id = id;
    this.icon = icon;
    this.age = age;
    this.dob = dob;
    this.town = town;
    this.instrument = instrument;
  }
}
