// ==========================================================================
// Member Info Box
// ==========================================================================

export default class Member {
  constructor(name, id, age, dob, town, instrument) {
    this.name = name;
    this.id = id;
    this.age = age;
    this.dob = dob;
    this.town = town;
    this.instrument = instrument;
  }
}
