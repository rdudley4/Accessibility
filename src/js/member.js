// ==========================================================================
// Member Info Box
// ==========================================================================

export default class Member {
  constructor(name, id, age, instrument) {
    this.name = name;
    this.id = id;
    this.age = age;
    this.instrument = instrument;
  }
}
